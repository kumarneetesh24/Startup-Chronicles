
<% @page_title="Articles" %>
<nav class="navbar navbar-default"  data-sr="wait 0.8s enter top ease-in 100px">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="row"  >
                 <a class="navbar-brand col-sm-3" href="/">Home</a> 
                </div>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                     <li>
                    	<% if session[:user_name] %>
						<%= link_to("Manage",{:controller =>'admin'}, :class =>"action" )%>
						<%else %>
						<%=link_to("login",{:controller =>'admin'}, :class =>"action" )%>
						<% end %>
                    </li>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
  <div class="container" style="background-color:white; min-height:90%;" >
	<div class="row">
<h1 data-sr="enter top ease-n "style="text-align:center;"><%=@public.title%></h1>
<hr class="star-primary" data-sr="enter top ease-n" >
<div class="small em text-center" ><%=@public.timestamp%></div>
<br>
<br>
<div class="col-lg-10 col-lg-offset-1" >
<p><%=@public.body.html_safe%></p>
</div>
</div>
 <div style="padding-bottom:4%">
 	<%= link_to("Back",{:action=>'index'},:class=>'btn btn-primary',:style=>"position:relative;left:45%") %>
 </div>
</div>
<footer>
<div class="footer-below navbar-fixed">
           
                <div class="row text-center">
                    <div class="col-lg-12" >
                        Copyright &copy; Startup Chronicles 2015
                    </div>
                </div>
            </div>
        </div>
        </footer>
