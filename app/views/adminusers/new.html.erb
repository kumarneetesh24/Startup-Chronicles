<div class="page-header"><h1>Admins</h1></div>
<%= link_to("back to admin",{:action =>"index"}, :class =>"action index") %>

<div class="create_article">
<h3>create article</h3>
<%= form_for(:adminusers,:html =>{:id => "users_form"}, :url=>{:action =>'create'}) do |f| %>
 <%= render(:partial => 'form',:locals =>{:f => f}) %>
 <div class="form-submit">
 	<%= submit_tag("create admin") %>
 </div>
 <% end %>
</div>